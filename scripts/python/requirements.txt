# Core LangChain for document processing and RAG
langchain>=0.3.0
langchain-community>=0.3.0
langchain-qdrant>=0.1.0

# Qdrant client for vector database operations
qdrant-client>=1.9.0

# Document loaders
pypdf>=4.0.0           # PDF parsing
pdfplumber>=0.11.0     # Better PDF table extraction (fallback)
python-docx>=1.1.0     # .docx files
openpyxl>=3.1.0        # .xlsx files
pandas>=2.0.0          # Data manipulation for Excel

# Embeddings (alternative to Ollama if needed)
sentence-transformers>=2.7.0

# Optional: for better text chunking
tiktoken>=0.7.0        # Token counting for OpenAI models
